require('chai').should()


describe('USER STORY 1', () => {
    describe('I ', () => {
        it('It should create a shopping list', () => {
            const shoppinglist = 1
            shoppinglist.should.equal(2)
        })
        it('The shopping list need to have a name', () => {
            
        })        
    })
})